# SNS投稿テーマジェネレーターくん 開発記録

## 背景

100アプリチャレンジ #5。ロードマップ上の元案は「謝罪文ジェネレーター」だが、ユーザーの実運用ニーズに合わせて変更。

### 解決する課題
精神疾患を持つ方に寄り添うInstagramアカウント [@tech.expert.takatsuki](https://www.instagram.com/tech.expert.takatsuki) の投稿テーマをネタ切れせず継続生成する。

### コンテンツスタイル
「こんな時はどうしてる？」というテーマに対し、当事者（事業所利用者）が回答していくスタイル。

---

## 要件整理の流れ

### 初期案（汎用SNSツール）
プラットフォーム選択（X / Instagram / TikTok / note）、トーン選択、生成数選択を持つ汎用ツールとして設計開始。

### 方向転換
ユーザーから運営アカウントと既存テーマ例（11個）を共有され、用途が「特定アカウントの投稿テーマ生成」と判明。汎用ツールではなく、このアカウント専用の特化ツールに方針変更。

### 既存テーマから抽出したスタイル
ユーザー共有の既存テーマを分析し、7種のスタイルパターンを特定：

| スタイル | 例 |
|---|---|
| 〜なあなたへ | 人に頼るのが苦手な、あなたへ |
| どうしてる？ | 罪悪感、どうしてる？ |
| 〜な時の〜 | 朝、エンジンがかからない時のスイッチ |
| 気づき | 自分のペースでいい、って気づいた瞬間 |
| 日常シェア | 久しぶりに外に出られた日のこと |
| 実用Tips | 薬を飲み忘れないための、私のルーティン |
| 参加招待 | 毎日が変わる"セルフケア"始めてみませんか？ |

これらをバランスよく混在させる指示をプロンプトに入れることで、毎回バリエーションが出る設計。

---

## 主な設計判断

### センシティブ表現への対応
- **プロンプトには明示しない**（過剰な制約による創造性の低下を避けるため）
- **生成結果からNGワードを含むテーマをフィルタで除外**する後処理方式を採用

NGワード:
- 死、死にたい
- 消えたい
- 終わりにしたい
- 自殺
- リストカット、リスカ

10案生成 → NGワードフィルタ → 残り（例：8案）を表示。

### カテゴリ未選択時の挙動
全カテゴリから自動生成する（エラーにしない）。

### 履歴管理
localStorageに採用テーマを保存し、次回起動時に「過去テーマ」欄へ自動投入することで重複回避の手間を削減。

### テーマ1案あたりの文字数
25文字以内（プロンプトで指定）。

---

## 最終仕様

### 入力
| フィールド | 仕様 |
|---|---|
| カテゴリ | チェックボックス（感情/日常生活/対人関係/セルフケア/仕事/休息）。未選択時は全カテゴリから自動生成 |
| 過去に使ったテーマ | テキストエリア（任意、最大3000文字） |
| 生成数 | 10案固定 |

### プロンプト
- アカウントコンセプト固定埋め込み（精神疾患当事者への寄り添い）
- 7スタイルをバランスよく混在
- 1案25文字以内
- 過去テーマ入力時のみ「これらと被らせない」と指示
- センシティブ配慮の制約は明示しない

### 出力
- 10案生成 → NGワードフィルタ → 残りを表示
- ストリーミング表示
- 個別コピー + 全部まとめてコピー
- 再生成ボタン（同じ条件でもう10案）
- localStorageに採用テーマ履歴を保存

### 技術スタック
- Next.js 16 / TypeScript / Tailwind v4
- Vercel AI SDK（Geminiデフォルト、Claude/GPT切替可）
- `00_template/` のデザインシステム適用
